import { Rope } from './rope';

describe('Rope Data Structure', () => {
  test('should concatenate two ropes', () => {
    new Rope();
  });

  test('should insert a string into the rope', () => {
  });

  test('should delete a substring from the rope', () => {
  });

  test('should extract a substring from the rope', () => {
  });
});
